const { PrismaClient } = require('@prisma/client'); const jwt = require('jsonwebtoken'); require('dotenv').config(); const prisma = new PrismaClient(); async function run() { const user = await prisma.user.findUnique({ where: { email: 'system@bank.com' } }); if (!user) { console.log('Admin user not found'); return; } const token = jwt.sign({ id: user.id }, process.env.JWT_SECRET || 'your-secret-key-change-this', { expiresIn: '1h' }); console.log('Admin Token:', token); } run().catch(console.error).finally(() => prisma.$disconnect());
